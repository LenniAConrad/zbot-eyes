# Secrets

Place your ChatGLM API key in `secrets/chatglm_api_key.txt` (single line).
The scaffold value `YOUR_CHATGLM_API_KEY_HERE` is intentionally treated as missing.

Alternative: set the environment variable `CHATGLM_API_KEY`.

The `secrets/` folder is gitignored except this file and `.gitkeep`.
